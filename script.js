/*const video = document.getElementById('miVideo');
let lastScrollY = window.scrollY; // Almacenamos la posición inicial del scroll

function handleScroll() {
  const currentScrollY = window.scrollY;
  if (currentScrollY > lastScrollY) {
    // Si el usuario está scrolling hacia abajo
    video.pause();
  } else {
    // Si el usuario está scrolling hacia arriba
    video.play();
  }
  lastScrollY = currentScrollY; // Actualizamos la última posición de scroll
}
window.addEventListener('scroll', handleScroll); */